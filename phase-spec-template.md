# Phase {N} Spec: {Title}

**Status:** Draft
**Authors:** {author names}
**Based on:** {dependent documents}
**Date:** {YYYY-MM-DD}

---

## Goal

<!-- Guidance: explain in 2-3 paragraphs what this phase aims to achieve.
     Answer three questions:
     1. What can the user do after this phase that they couldn't before?
     2. What is the core technical objective?
     3. How does this phase relate to the previous one? -->

## Scope

<!-- Guidance: list explicit in-scope and out-of-scope items.
     Out of scope is as important as in scope — it prevents agents from
     expanding the phase without explicit approval. -->

### In scope

- {item}

### Out of scope

- {item}

## Runtime And Tooling

<!-- Guidance: what runtime and toolchain does this phase depend on?
     Are there new dependencies? Environment constraints? -->

## Architecture Constraints

<!-- Guidance: numbered design constraints that must not be violated.
     Constraints come from architecture docs, previous decisions, or this
     phase's design requirements. Each constraint should be a clear
     "must do X" or "must not do Y" statement. -->

1. {constraint}

## Public Interface Requirements

<!-- Guidance: what outward-facing interfaces does this phase introduce
     or change? CLI flags, API endpoints, library APIs, config formats,
     data schemas. -->

## {Domain-Specific Sections}

<!-- Guidance: add sections specific to this phase's domain. Examples:
     - Database schema changes
     - Auth flow changes
     - API route changes
     - Data pipeline changes
     Name each section concretely. Do not leave this as a placeholder. -->

## Testing Requirements

<!-- Guidance: testing strategy for this phase.
     - What unit tests are expected?
     - What integration or smoke tests?
     - Any environment-specific tests that can be skipped with reason? -->

## Completion Criteria

<!-- Guidance: binary, verifiable completion conditions.
     Each criterion should be checkable — "test suite passes" or
     "endpoint returns 200" rather than "performance is better."
     Each criterion should map to at least one taskboard task. -->

- {criterion}

## References

<!-- Guidance: list all documents referenced by this spec. -->
